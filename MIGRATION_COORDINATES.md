# Coordinate ownership

`@whiteslove/parsing-lexicon` owns text recognition, aliases and canonical names only.
All coordinates, bounds, OSM/Wikidata metadata and learned spatial anchors belong to `@whiteslove/geo-catalog`.
